apiVersion: v1
kind: Service
metadata:
  name: employees-service
  namespace: employees-space
spec:
  type: NodePort
  selector:
    app: app
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000
